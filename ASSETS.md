# ASSETS

Binary and generated assets in the host tree, with origin and licence.
Phases 1–6 add none: the institution is SQL, Deno and TypeScript.

Phase 7 migrates the floor from `bot-crossing/` into `src/views/lab/floor/`.
The assets it carries are catalogued in `bot-crossing/ASSETS.md` today; that
list moves here, entry by entry, when the standalone root is deleted.
